package vn.edu.ptit.znine.context;

import java.io.FileOutputStream;
import java.io.InputStream;
import java.nio.file.Path;

import org.springframework.web.bind.annotation.RequestParam;

public class ImageContext {
//	public String Image(String image) throws Exception {
//		Path filePart = RequestParam.getPart(image);
//		String fileName = filePart.getSubmittedFileName();
//		String savePath = "C:/path/to/save/directory/" + fileName;
//		
//		try (InputStream inputStream = filePart.getInputStream();
//				FileOutputStream outputStream = new FileOutputStream(savePath)) {
//			byte[] buffer = new byte[4096];
//			int bytesRead = -1;
//			while ((bytesRead = inputStream.read(buffer)) != -1) {
//				outputStream.write(buffer, 0, bytesRead);
//			}
//		}
		
//		String imageUrl = "http://your-domain.com/path/to/save/directory/" + fileName;
//	}

}
